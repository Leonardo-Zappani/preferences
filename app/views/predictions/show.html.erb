<div class="container mx-auto p-6  text-white">
  <h1 class="text-2xl font-semibold">Resultado:</h1>
  <p class="mt-2 <%= @prediction.dm_label ? 'text-red-600' : 'text-green-600' %>">
    <%= @prediction.dm_label ? "Positivo para Diabetes/Pre-DM" : "Negativo" %>
  </p>

  <%# If editing is allowed, re-render the form partial %>
  <div class="mt-6">
    <button type="button"
            onclick="window.location.href='<%= root_path %>'"
            class="w-full text-white py-3 bg-indigo-600 hover:bg-indigo-700 font-bold rounded-xl shadow-md hover:shadow-lg transition">
      Voltar
    </button>
  </div>

</div>
